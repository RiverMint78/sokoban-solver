# Sokoban Solver

从我的旧仓库找到的历史遗留 Python 练习项目，实现了一个简单的 Sokoban 关卡求解器，以及实现了 BFS / DFS / UCS / Greedy / A\*等经典搜索方法，比较好玩的是，A\* 可选基于指派的启发函数。

## 项目结构

- `main.py`：程序入口
- `solver.py`：搜索求解逻辑
- `game.py`：手动游戏逻辑
- `levels/`：示例关卡
- `util.py`：工具函数
